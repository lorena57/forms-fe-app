// import { FETCH_USER, NEW_USER } from '../actions/types';

const INITIAL_STATE = {
  authenticated: '',
  errorMessage: '',
};

// export default function (state = INITIAL_STATE, action) {
//   switch (action.type) {
//     default:
//       return state;
//   }
// }

export default function (state = INITIAL_STATE, action) {
  return state;
}
